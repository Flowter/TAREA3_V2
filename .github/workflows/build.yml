run-name: Workflow Análisis 
on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened] 
jobs:
  SAST:
    uses: Flowter/Tarea2_V2/.github/workflows/SAST.yml@main
    with:
      run-name: "Análisis de Código"
  SCA:
    needs: SAST
    uses: Flowter/Tarea2_V2/.github/workflows/SCA.yml@main
    with:
      run-name: "Análisis de Vulnerabilidades"
